## Smart Contract Wallets

Memswap is fully compatible with smart contract wallets

**EIP-1271**

Approve an order via call to isValidSignature() (stateless)

**Pre-Validation**

Contracts can “pre-validate” an order onchain, which means it can be filled later without requiring a signature
