## Onchain Orderbook

While not a hard requirement, Memswap is designed to work well with a fully onchain orderbook that uses the Ethereum mempool for intent distribution, instead of an offchain API.

Learn more about Intentful Approvals

Efficient Order Format > Designed to minimize gas usage when posting orders onchain

Post Order > Directly publish an onchain (just in calldata, to minimize gas)
